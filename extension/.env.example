# Extension Environment Variables
# Copy this file to .env and fill in the values before building.

# Sentry DSN for error tracking (injected by webpack as SENTRY_DSN_EXTENSION)
SENTRY_DSN=your_sentry_dsn_here

# Node environment - controls build mode and logging (development | production | test)
NODE_ENV=development
